{
  "name": "opedd-contracts",
  "version": "1.0.0",
  "description": "Opedd Protocol — On-chain license registry on Base",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:testnet": "npx hardhat run scripts/deploy.ts --network baseSepolia",
    "deploy:mainnet": "npx hardhat run scripts/deploy.ts --network base",
    "verify": "npx hardhat verify --network base"
  },
  "keywords": [
    "opedd",
    "licensing",
    "base",
    "blockchain"
  ],
  "author": "Opedd",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.2",
    "hardhat": "^3.1.8"
  },
  "dependencies": {
    "@coinbase/cdp-sdk": "^1.44.1",
    "@coinbase/coinbase-sdk": "^0.25.0",
    "dotenv": "^17.3.1"
  }
}
